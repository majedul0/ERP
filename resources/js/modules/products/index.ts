/**
 * Products module — registering and listing what the company sells, and
 * moving stock on and off the shelf.
 *
 * Import from the barrel (`@/modules/products`), not from files inside it.
 */
export { StockMovementModal } from './components/stock-movement-modal';
export type * from './types';
