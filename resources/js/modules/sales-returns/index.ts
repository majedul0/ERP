/**
 * Sales returns module — goods a distributor sent back.
 *
 * Import from the barrel (`@/modules/sales-returns`), not from files inside it.
 */
export { DeleteReturnDialog } from './components/delete-return-dialog';
export { default as ReturnDocument } from './components/return-document';
export { default as ReturnForm } from './components/return-form';
export type * from './types';
