/**
 * Finance module — expenses and the reports that read across the whole system.
 *
 * Import from the barrel (`@/modules/finance`), not from files inside it.
 */
export { ExpenseForm } from './components/expense-form';
export type * from './types';
