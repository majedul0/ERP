/**
 * Distributors module — who the company sells to.
 *
 * Import from the barrel (`@/modules/distributors`), not from files inside it.
 * The distributor *types* still live in `@/modules/invoices`, where the invoice
 * form consumes them.
 */
export { DeleteDistributorDialog } from './components/delete-distributor-dialog';
